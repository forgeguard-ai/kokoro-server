{{- if and .Values.kokoroTTS.apiKey.enabled .Values.kokoroTTS.apiKey.value -}}
{{/*
Only rendered when the key is supplied as a literal. With `existingSecret` the
operator owns the Secret and this chart must not create, and therefore must not
later delete, an object it does not own.
*/}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-api-key" (include "kokoro-server.fullname" .) }}
  labels:
    {{- include "kokoro-server.labels" . | nindent 4 }}
type: Opaque
stringData:
  api-key: {{ .Values.kokoroTTS.apiKey.value | quote }}
{{- end }}
